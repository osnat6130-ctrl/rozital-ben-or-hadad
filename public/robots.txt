User-agent: *
Allow: /

# /admin לא נחסם כאן במכוון: הוא מסומן noindex בכותרת X-Robots-Tag
# (ראו public/_headers), וכדי שסורק יראה את הכותרת הוא חייב להיות מורשה
# לבקש את הדף. חסימה ב-robots.txt הייתה מונעת ממנו לראות את ה-noindex.

Sitemap: https://rozital.co.il/sitemap.xml
